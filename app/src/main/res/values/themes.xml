<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ============================================
         TEMA PRINCIPAL CLARO
         ============================================ -->
    <style name="Theme.IBSSBIVLE" parent="Theme.Material3.Light.NoActionBar">
        <!-- Cores Primárias -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorPrimaryVariant">@color/primary_variant_light</item>
        <item name="colorSecondary">@color/secondary_light</item>
        <item name="colorSecondaryVariant">@color/secondary_variant_light</item>
        
        <!-- Cores de Background -->
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorSurface">@color/surface_light</item>
        
        <!-- Cores de Texto -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorOnBackground">@color/text_primary_light</item>
        <item name="colorOnSurface">@color/text_primary_light</item>
        
        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/background_light</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/background_light</item>
        <item name="android:windowLightNavigationBar">true</item>
        
        <!-- Outras configurações -->
        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:textColorPrimary">@color/text_primary_light</item>
        <item name="android:textColorSecondary">@color/text_secondary_light</item>
    </style>

    <!-- ============================================
         TEMA ESCURO
         ============================================ -->
    <style name="Theme.IBSSBIVLE.Dark" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Cores Primárias -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryVariant">@color/primary_variant_dark</item>
        <item name="colorSecondary">@color/secondary_dark</item>
        <item name="colorSecondaryVariant">@color/secondary_variant_dark</item>
        
        <!-- Cores de Background -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        
        <!-- Cores de Texto -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorOnBackground">@color/text_primary_dark</item>
        <item name="colorOnSurface">@color/text_primary_dark</item>
        
        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/background_dark</item>
        <item name="android:windowLightNavigationBar">false</item>
        
        <!-- Outras configurações -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:textColorPrimary">@color/text_primary_dark</item>
        <item name="android:textColorSecondary">@color/text_secondary_dark</item>
    </style>

    <!-- ============================================
         TEMA SÉPIA
         ============================================ -->
    <style name="Theme.IBSSBIVLE.Sepia" parent="Theme.Material3.Light.NoActionBar">
        <!-- Cores Primárias -->
        <item name="colorPrimary">@color/primary_sepia</item>
        <item name="colorPrimaryVariant">@color/primary_variant_sepia</item>
        <item name="colorSecondary">@color/secondary_sepia</item>
        <item name="colorSecondaryVariant">@color/secondary_variant_sepia</item>
        
        <!-- Cores de Background -->
        <item name="android:colorBackground">@color/background_sepia</item>
        <item name="colorSurface">@color/surface_sepia</item>
        
        <!-- Cores de Texto -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorOnBackground">@color/text_primary_sepia</item>
        <item name="colorOnSurface">@color/text_primary_sepia</item>
        
        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/background_sepia</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/background_sepia</item>
        <item name="android:windowLightNavigationBar">true</item>
        
        <!-- Outras configurações -->
        <item name="android:windowBackground">@color/background_sepia</item>
        <item name="android:textColorPrimary">@color/text_primary_sepia</item>
        <item name="android:textColorSecondary">@color/text_secondary_sepia</item>
    </style>
</resources>