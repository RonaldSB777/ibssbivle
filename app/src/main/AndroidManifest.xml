<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ibscc.bible">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="Bíblia Sagrada"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- ACTIVITY PRINCIPAL -->
        <activity 
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OUTRAS ACTIVITIES -->
        <activity android:name=".ChapterActivity" />
        <activity android:name=".VerseActivity" />
        <activity android:name=".QuizMenuActivity" />
        <activity android:name=".QuizActivity" />

        <!-- ⭐ ACTIVITY DE ADMIN -->
        <activity
            android:name=".AdminProgramacaoActivity"
            android:label="Admin - Programação" />

        <!-- RECEIVER PARA NOTIFICAÇÕES -->
        <receiver 
            android:name=".DailyNotificationHelper"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>